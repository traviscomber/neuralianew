{
  "buildCommand": "npm ci --force && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm ci --force",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
