Recognition image helpers for sections that have not yet moved to stable canonical runtime assets.

Section 04 is fully canonicalized and must not be represented here. Its only runtime artwork URL is `/recognition-section04-platform-final.webp`, rendered by `components/recognition-section04-final.tsx` as a native image. The route `app/recognition-section04-platform-final.webp/route.ts` reconstructs and serves the exact approved WebP bytes from the text-safe payload modules in `lib/recognition-section04-final-asset/`.

Section 04 must remain WebP-based; do not replace it with inline SVG artwork.
