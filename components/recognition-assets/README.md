Recognition image helpers for sections that have not yet moved to stable canonical runtime assets.

Section 04 is fully canonicalized and must not be represented here. Its only runtime artwork URL is `/recognition-section04-platform-canonical.webp`, served by `app/recognition-section04-platform-canonical.webp/route.ts` from the canonical portrait WebP payload in `lib/recognition-section04-asset/`.
