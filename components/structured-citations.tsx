// This component is kept for backward compatibility but no longer renders scripts
// Structured data is now handled via Next.js metadata in layout.tsx
export function StructuredCitations() {
  return null
}
