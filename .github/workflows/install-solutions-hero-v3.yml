name: Install canonical solutions hero v3

on:
  push:
    paths:
      - '.github/workflows/install-solutions-hero-v3.yml'

permissions:
  contents: write

jobs:
  install:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Rebuild canonical WebP
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p public/images/solutions
          cat .asset-staging/solutions-hero-v3/part-*.b64 | tr -d '\n' | base64 -d > public/images/solutions/hero-system.webp
          test "$(wc -c < public/images/solutions/hero-system.webp)" -eq 101364
          file public/images/solutions/hero-system.webp | grep -q 'Web/P image'

      - name: Commit production asset and clean staging
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .asset-staging/solutions-hero-v3
          rm -rf .asset-staging/solutions-hero-v2
          rm -f .github/workflows/install-solutions-hero-v3.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix(solutions): install high-quality canonical teal hero'
          git push origin HEAD:main

# trigger after final chunk verification
